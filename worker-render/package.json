{
  "name": "chairside-worker-render",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Holds the YouCam key so Mirror can render a client's own scan without shipping a credential to the browser",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.128.0"
  }
}
